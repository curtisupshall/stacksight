import { ISearchResponse } from "@/server/services/search-service"
import { Box, List, ListItem, ListItemText, Menu, MenuItem, MenuList, Typography } from "@mui/material"
import NextLink from "next/link";
import { useMemo } from "react";

interface ISearchResultsProps {
    searchResults: ISearchResponse
}

export default function SearchResults(props: ISearchResultsProps) {
    

    return (
        <>
            
        </>
    )
}
